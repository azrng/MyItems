# 物品库无保质期占位修复

## 本次目标

- 修复物品库中无保质期物品右侧显示空白到期状态胶囊的问题。

## 核心改动

- 在 `ItemLibraryPage` 中复用 `HasValueConverter`。
- 为列表卡片右侧到期状态胶囊增加 `ExpiryDate` 可见性判断。
- 无保质期物品不再渲染空白状态容器。

## 修改文件

- `src/MyItems/Views/ItemLibraryPage.xaml`
- `TASK.md`

## 校验情况

- 已执行：`dotnet build .\src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，通过。

## 风险或遗留项

- 未执行 Android 真机截图验证，当前命令行环境缺少 Android SDK。
- Windows 编译剩余 MVVM Toolkit WinRT/AOT 警告为既有提示，与本次显示条件修复无直接关系。
