# 添加页日均成本开关修复

## 本次目标
- 修复添加物品页“记录日均成本”只有说明文字、缺少可见开关的问题。

## 核心改动
- 将添加页日均成本设置区改为左侧说明、右侧原生 `Switch` 的布局。
- `Switch` 继续绑定 `TrackDailyCost`，保存时沿用现有 DTO 写入链路。
- 移除添加页中不再使用的 Syncfusion toolkit 和 model 命名空间引用。

## 修改文件
- `src/MyItems/Views/AddItemPage.xaml`
- `TASK.md`

## 校验情况
- 已执行：`git diff --check -- TASK.md src\MyItems\Views\AddItemPage.xaml`，未发现空白错误。
- 已执行：`dotnet build .\src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，编译通过。

## 风险或遗留项
- 本次未执行 Android 真机界面 smoke test；本机缺少 Android SDK，需在 Android 环境中确认实际渲染。
- 编译时工作区还存在分类管理相关未提交改动，已避开提交，但本次编译包含当前工作区整体状态。
