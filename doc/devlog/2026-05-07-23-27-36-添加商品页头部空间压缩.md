# 添加商品页头部空间压缩

## 本次目标
- 检查并压缩添加商品页面顶部空闲空间，使页面头部结构与物品详情页保持一致。

## 核心改动
- 将添加页页面内头部卡片调整为详情页同款结构，仅保留图标、标题、副标题、状态与分类信息。
- 移除头部卡片内的自定义返回按钮，返回交给 Shell 顶栏统一处理。
- 移除顶部大面积红色背景层，避免 Shell 顶栏下方继续出现整块空闲区域。

## 修改文件
- `src/MyItems/Views/AddItemPage.xaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build .\src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0 --no-restore`，构建通过，0 个错误。
- 构建仍有既有 `MVVMTK0045` 警告，本次未处理。
- Android 真机视觉效果仍需在设备侧确认。

## 风险或遗留项
- 本次只调整布局，不修改保存、回填和数据持久化链路。
- 当前环境未配置 Android SDK，无法在本机直接完成 Android 构建或真机截图验证。
