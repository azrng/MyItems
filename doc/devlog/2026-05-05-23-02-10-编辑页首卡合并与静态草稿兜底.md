# 编辑页首卡合并与静态草稿兜底

## 本次目标
- 继续压缩编辑页顶部留白。
- 为详情页进入编辑页再补一条不依赖路由 payload 或 DI 的旧值回填兜底。

## 核心改动
- 新增 `EditItemDraftStore` 静态草稿存储，详情页点击编辑时同步写入，编辑页优先消费。
- 保留原有服务草稿与 `editDraft` 查询参数三条链路并行，降低单一路径失效风险。
- 将页头并入首张编辑信息卡，去掉卡片外独立标题区域，减少顶部空场。

## 修改文件
- `src/MyItems/Helpers/EditItemDraftStore.cs`
- `src/MyItems/ViewModels/ItemDetailViewModel.cs`
- `src/MyItems/ViewModels/AddItemViewModel.cs`
- `src/MyItems/Views/AddItemPage.xaml`

## 校验情况
- 执行 `dotnet build src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`
- 结果：通过，0 错误
- 项目仍有既有 `MVVMTK0045` 警告

## 风险或遗留项
- 仍未在 Android 真机上完成“详情页 -> 编辑页”的实机验证。
