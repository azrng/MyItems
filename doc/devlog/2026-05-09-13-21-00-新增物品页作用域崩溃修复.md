# 新增物品页作用域崩溃修复

## 本次目标

- 修复 Flutter 版本从物品库或抽屉进入新增物品页时 `AppScope not found` 的断言崩溃。

## 核心改动

- 将 `AppScope` 从 `MaterialApp.home` 内部上移到 `MaterialApp` 外层，确保 `Navigator.push` 新建的 Route 也能继承同一个 `AppStore`。
- 补充 widget 回归测试，覆盖从物品库切换到新增物品页的导航链路。
- 更新 `TASK.md`，新增 T054 并标记为 `DONE`。

## 修改文件

- `lib/main.dart`
- `test/navigation_test.dart`
- `TASK.md`

## 校验情况

- 已执行：`flutter test test/navigation_test.dart`
- 结果：2 个导航测试全部通过。
- 已执行：`flutter analyze`
- 结果：通过，无问题。
- 已执行：`flutter test`
- 结果：6 个测试全部通过。
- 已执行：`flutter build apk --debug`
- 结果：首次因当前 PowerShell 未刷新 `JAVA_HOME` 失败；刷新 `JAVA_HOME`、Android SDK 和镜像环境变量后重新执行成功，生成 `build\app\outputs\flutter-apk\app-debug.apk`。

## 风险或遗留项

- 本次只调整全局状态作用域挂载位置，不改变 `AppStore` 实例生命周期和页面业务逻辑。
