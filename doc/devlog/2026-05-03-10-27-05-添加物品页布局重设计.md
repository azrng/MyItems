# 添加物品页布局重设计

## 本次目标
- 使用前端设计 skill 分析添加物品页现有录入体验，并重新设计移动端表单布局。

## 核心改动
- 将原有单列流水式表单调整为“基础信息”“购买与存放”“期限与数量”三段卡片式结构。
- 将扫码入口收束到基础信息区右侧，减少页面顶部独立按钮占位。
- 将错误提示移动到底部固定保存区，让校验反馈与提交动作保持在同一操作区域。
- 补充表单卡片、底部操作区、日期选择器、备注输入框和紧凑按钮的样式资源。

## 修改文件
- `src/MyItems/Views/AddItemPage.xaml`
- `src/MyItems/Resources/Styles/Styles.xaml`
- `src/MyItems/Resources/Styles/Sizes.xaml`
- `TASK.md`

## 校验情况
- 已执行：`dotnet build .\src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，通过，0 警告 0 错误。
- 已执行：`dotnet build .\MyItems.sln`，未通过；Android 目标因本机找不到 Android SDK 报 `XA5300`，Windows 目标已进入编译并未暴露本次 XAML 错误。
- 已回读修改文件，关键中文显示正常。

## 风险或遗留项
- 未在 Android / iOS 真机或模拟器上做视觉 smoke test；需在具备 Android SDK 或移动端环境后确认卡片间距、底部保存区与键盘遮挡表现。
