# 编辑页头部布局对齐详情页

## 本次目标
- 按用户截图反馈，将编辑页头部结构调整为与物品详情页一致的头部卡片布局。

## 核心改动
- 编辑页头部卡片改为详情页同款的左侧圆形区域 + 右侧标题、副标题、状态标签结构。
- 将物品名称和分类字段从头部卡片移到“基础信息”卡片第一行。
- “基础信息”卡片扩展为三行两列，继续承载品牌、存放位置、数量、条码字段。

## 修改文件
- `src/MyItems/Views/AddItemPage.xaml`

## 校验情况
- 已执行：`dotnet build .\src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0 --no-restore`，编译通过。

## 风险或遗留项
- Android 真机视觉仍需用户侧截图确认。
- 编译仍有既有 `MVVMTK0045` 警告，本次未处理。
