# Android 发布明确生成 APK

## 本次目标
- 调整 GitHub Actions Android 发布流程，明确生成 APK 包。

## 核心改动
- 在 `.github/workflows/release.yml` 的 `dotnet publish` 命令中新增 `-p:AndroidPackageFormat=apk`。
- 保持现有 APK 查找、上传 artifact 和发布 GitHub Release 的链路不变。

## 修改文件
- `.github/workflows/release.yml`
- `TASK.md`

## 校验情况
- 已重新读取 workflow，确认发布命令包含 `AndroidPackageFormat=apk`。
- 已执行 `git diff --check`，未发现空白错误。
- 未在本机执行 Android 发布构建；当前环境缺少 Android SDK，真实 APK 产物需由 GitHub Actions 或具备 Android SDK 的环境验证。

## 风险或遗留项
- 当前签名参数仍为空字符串，后续如需正式分发，应通过 GitHub Secrets 配置 keystore 签名。
