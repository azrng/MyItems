# 物品库状态胶囊显示修复

## 本次目标
- 修复物品库列表卡片右侧到期状态胶囊在移动端被挤压，导致文字显示不完整的问题。

## 核心改动
- 为物品库列表右侧到期状态胶囊增加最小宽度。
- 将状态文字改为单行不换行并居中显示。
- 将左侧购买信息限制为单行截断，避免挤压右侧状态胶囊。

## 修改文件
- `TASK.md`
- `src/MyItems/Views/ItemLibraryPage.xaml`

## 校验情况
- 已执行：`dotnet build src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，通过，0 错误。
- 构建仍有既有 `MVVMTK0045` 警告，和本次 XAML 布局修复无直接关系。

## 风险或遗留项
- 未在 Android 真机截图复验，建议后续查看物品库中“临期 4 天”“过期 4 天”等状态胶囊是否完整显示。
