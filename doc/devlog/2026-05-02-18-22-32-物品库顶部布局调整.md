# 物品库顶部布局调整

## 本次目标
- 按截图调整物品库顶部区域，删除重复标题，并将菜单按钮移动到左上角。

## 核心改动
- 删除物品库内容区内重复的“物品库”标题。
- 保留 Shell 顶部标题，将设置菜单按钮移动到内容区左上角。
- 搜索栏、统计卡片、分类筛选与列表顺序保持不变。

## 修改文件
- `src/MyItems/Views/ItemLibraryPage.xaml`
- `TASK.md`

## 校验情况
- 已执行：`git diff --check -- TASK.md src\MyItems\Views\ItemLibraryPage.xaml`，未发现空白错误。
- 当前工作区直接构建被未提交的 `MockDataService.cs` 改动阻断，错误为重复定义 `itemBatches`。
- 已在临时干净 worktree 中仅套用本次改动后执行：`dotnet build src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，编译通过。

## 风险或遗留项
- 未执行 Android 真机界面 smoke test；需要在目标设备确认按钮位置与截图一致。
- 当前工作区仍有与本次任务无关的未提交改动，未纳入本次提交。
