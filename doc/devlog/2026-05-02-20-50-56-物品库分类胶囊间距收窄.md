# 物品库分类胶囊间距收窄

## 本次目标
- 保持当前横向分类胶囊方案，仅收窄分类之间的视觉间隔。

## 核心改动
- 将物品库分类 `SfChipGroup` 的 `ChipPadding` 调整为 `4,4`，让分类项排列更紧凑。

## 修改文件
- `src/MyItems/Views/ItemLibraryPage.xaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，构建通过。
- 已执行 `git diff --check -- TASK.md src\MyItems\Views\ItemLibraryPage.xaml`，未发现空白错误。

## 风险或遗留项
- 未执行 Android 真机界面 smoke test；本次为分类间距微调，已通过 Windows 目标框架编译校验。
- 构建仍存在项目既有的 MVVMTK0045 与过时 API 警告，本次未处理。
