# 物品库标题栏设置按钮弱化

## 本次目标
- 调整物品库标题栏设置按钮的视觉权重，去除与深色标题栏冲突的浅蓝背景。

## 核心改动
- 新增 `TitleIconButton` 样式，使用标题栏同色背景与白色图标。
- 物品库标题栏设置按钮改用 `TitleIconButton`，保留原有点击热区和交互入口。

## 修改文件
- `src/MyItems/Resources/Styles/Styles.xaml`
- `src/MyItems/Views/ItemLibraryPage.xaml`
- `TASK.md`

## 校验情况
- 已执行 `dotnet build src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，构建通过。
- 已执行 `git diff --check -- TASK.md src\MyItems\Resources\Styles\Styles.xaml src\MyItems\Views\ItemLibraryPage.xaml`，未发现空白错误。

## 风险或遗留项
- 未执行 Android 真机界面 smoke test；本次为标题栏按钮样式微调，已通过 Windows 目标框架编译校验。
- 构建仍存在项目既有的 MVVMTK0045 与过时 API 警告，本次未处理。
