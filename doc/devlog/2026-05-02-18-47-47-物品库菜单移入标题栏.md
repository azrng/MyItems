# 物品库菜单移入标题栏

## 本次目标
- 将物品库菜单按钮移动到标题栏左侧，避免占用内容区顶部空间。

## 核心改动
- 为物品库页新增 `Shell.TitleView`，在标题栏内放置菜单按钮和“物品库”标题。
- 删除内容区原来的菜单按钮行。
- 将搜索栏、统计卡片、分类筛选和列表整体上移一行。

## 修改文件
- `src/MyItems/Views/ItemLibraryPage.xaml`
- `TASK.md`

## 校验情况
- 已执行：`git diff --check -- TASK.md src\MyItems\Views\ItemLibraryPage.xaml`，未发现空白错误。
- 已在临时干净 worktree 中仅套用本次改动后执行：`dotnet build src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，编译通过。

## 风险或遗留项
- 未执行 Android 真机界面 smoke test；需要在目标设备确认标题栏左侧按钮的实际位置和点击区域。
- 当前工作区仍有与本次任务无关的未提交改动，未纳入本次提交。
