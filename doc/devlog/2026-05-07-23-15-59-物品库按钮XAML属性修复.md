# 物品库按钮 XAML 属性修复

## 本次目标

修复构建时报出的 `HorizontalContentAlignment` 属性不存在错误。

## 核心改动

- 移除物品库设置面板 4 个按钮上的 `HorizontalContentAlignment="Start"`。
- 保留按钮现有 `HorizontalOptions="Fill"`、`Padding`、高度与圆角样式，避免引入新的控件结构。

## 修改文件

- `src/MyItems/Views/ItemLibraryPage.xaml`

## 校验情况

- 已执行：`dotnet build .\src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0 --no-restore`
- 结果：构建通过，0 个错误。
- 已复查：`HorizontalContentAlignment` / `VerticalContentAlignment` 在项目 XAML 与 C# 中无残留。
- 仍有既有 `MVVMTK0045` warning，本次未处理。

## 风险或遗留项

- 当前环境缺少 Android SDK，未能执行 Android 目标构建。
- 移除内容对齐属性后，按钮文本对齐依赖 MAUI 默认表现与 Padding；若真机视觉仍需左对齐，可后续改为 `Border + Label + TapGestureRecognizer` 的显式布局。
