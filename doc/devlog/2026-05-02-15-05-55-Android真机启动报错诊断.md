# Android 真机启动报错诊断

## 本次目标
- 排查 Visual Studio 连接 Android API27 真机后启动失败的本地原因。

## 核心改动
- 新增任务记录 `T002`，记录真机启动诊断任务。
- 为项目补充 MAUI 应用图标资源，修复 `AndroidManifest.xml` 引用 `@mipmap/appicon` / `@mipmap/appicon_round` 但资源未生成的问题。
- 设置 `WindowsPackageType=None`，并补充 Windows 平台启动入口，修复 Windows 目标缺少 AppxManifest 和入口点的问题。
- 命令行 Android 构建仍受本机 shell 未识别 Android SDK 目录影响，需在 Visual Studio 中复测真机启动。

## 修改文件
- `TASK.md`
- `doc/devlog/2026-05-02-15-05-55-Android真机启动报错诊断.md`
- `src/MyItems/MyItems.csproj`
- `src/MyItems/Resources/AppIcon/appicon.svg`
- `src/MyItems/Resources/AppIcon/appiconfg.svg`
- `src/MyItems/Platforms/Windows/App.xaml`
- `src/MyItems/Platforms/Windows/App.xaml.cs`

## 校验情况
- 已执行 `dotnet --info`，确认当前 SDK 为 .NET 10 preview，已安装 MAUI/Android workload。
- 已执行 `dotnet workload list`，确认存在 `maui-android` / `android` workload。
- 已执行 `dotnet build .\src\MyItems\MyItems.csproj -f net10.0-android`，失败于 `XA5300: 找不到 Android SDK 目录`。
- 已检查常见 Android SDK 目录、`ANDROID_HOME` / `ANDROID_SDK_ROOT` 环境变量和 `adb` / `sdkmanager` 命令，均未发现可用 SDK/ADB。
- 用户提供的 `C:\Users\Azrng\.android\avd\pixel_7_-_api_26_0.avd` 已确认是模拟器 AVD 配置目录，不是 Android SDK 目录；该 AVD 配置目标为 `android-26`。
- 已执行 `dotnet build .\src\MyItems\MyItems.csproj -f net10.0-windows10.0.19041.0`，构建成功；仍存在既有 `DisplayAlert` 过时警告和 CommunityToolkit WinRT AOT 警告。

## 风险或遗留项
- 当前无法通过命令行完成 Android 构建、部署和真机启动验证，因为 shell 仍未识别 Android SDK 目录。
- 需要在 Visual Studio 中重新生成并启动 Android 目标，确认 manifest 图标资源错误已消失。
- SDK 问题修复后，如仍启动失败，需要继续收集 VS 输出窗口、adb logcat 或异常堆栈。
